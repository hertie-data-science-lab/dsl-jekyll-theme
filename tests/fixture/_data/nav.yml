# The tab bar `_includes/nav.html` reads. A consuming site generates its own; this is the
# minimum shape that proves the include renders one.
items:
- url: /
  name: Home
  icon_class: fa fa-home fa-lg

- url: /a-page/
  name: A page
  icon_class: fas fa-file-alt
