---
layout: page
title: A page
permalink: /a-page/
---

The `page` layout, inside `default` - so this exercises the head, the header, the nav and
the footer, and every partial in `_sass` through `/assets/css/main.css`.

A [link](/), some `code`, and a list:

- one
- two
